function [normalizedVar] = normalizeVariance(objectDataMean, objectDataVar)
%normalizeVariance - Normalizes an object's data's variance using the mean

normalizedVar =  objectDataVar %TODO: -  change this to be legit. do variance/(mean of squares)

end